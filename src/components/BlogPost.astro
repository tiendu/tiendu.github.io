---
const { title, url, pinned } = Astro.props;
---
<li class={`post ${pinned ? 'is-pinned' : ''}`}>
  <a href={url}>
    {title}
    {pinned ? <sup class="pin">📌</sup> : null}
  </a>
</li>

<style>
  .post { list-style: none; }
  .pin { margin-left: .4rem; font-size: .9em; }
</style>
